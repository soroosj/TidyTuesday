---
title: "Crop Yields"
author: "Joel Soroos"
date: "9/30/2020"
output: html_document
---


##1. Source data
```{r source, warning = FALSE, message = FALSE}

   library(tidytuesdayR)
   library(janitor)

   tuesdata <- tidytuesdayR::tt_load('2020-09-01') 
   
   crops_raw <- tuesdata$key_crop_yields %>%
      clean_names()
```


##2. Exploratory Data Analysis
```{r skim, warning = F, message =F}

   library(skimr)

   #skim (crops_raw)
```


##3a. Transform data
```{r transform, message = F}

   library(tidyverse)
   library (glue)

   crops <- crops_raw %>%
      pivot_longer (cols = 4:14, names_to = "commodity", values_to = "yield") %>%
      rename (country = entity) %>%
      mutate (
         commodity = if_else (commodity == "cocoa_beans_tonnes_per_hectare", word(commodity, 1, sep = "_"), word (commodity, sep = "_")),
         commodity = str_to_title(commodity),
         decade = glue("d_{floor(year/10)*10}s")
         ) %>%
      drop_na () %>%
      filter (commodity == "Bananas")%>%
      select (country, commodity, decade, year, yield)

#https://gist.github.com/jthomasmock/5bba85b2ef3fdad94dce9c428b092a93
#https://pbs.twimg.com/media/EiEhaOLXcAA5On3?format=jpg&name=4096x4096
```


##3b. Calculate medians by decade
```{r}
   
   crops_mean <- crops %>%
      group_by (country, decade) %>%
      summarize (mean = round(mean (yield),1)) %>%
      pivot_wider (names_from = decade, values_from = mean) %>%
      ungroup() %>%
      rowwise(country) %>%
      mutate (
         mean = mean (c(d_1960s,d_1970s,d_1980s,d_1990s,d_2000s,d_2010s), na.rm = T),
         mean = round (mean, 1)
         ) %>%
      arrange (-mean) %>%
      relocate (mean,.after = country) %>%
      head (20)
```


#4.  Visualizing
```{r}

   library(gt)
   library(here)

   crops_mean %>%
      ungroup() %>%
      gt () %>%
         data_color(
            columns = 2:8,
            colors = scales::col_numeric(
               palette = c("#ffffff", "#f2fbd2", "#c9ecb4", "#93d3ab", "#35b0ab"),
               na.color = "gray94",
               domain = NULL
               ) 
            ) %>%
         tab_header (title =  md("**Banana Mean Yields 1960-2018**")) %>%
         tab_source_note ("Table: Joel Soroos @soroosj  | Data: Our World in Data") %>%
         tab_spanner(
           label = md("**By Decade**"),
           columns = 3:8
            ) %>%
         tab_style(
            style = list(cell_text(weight = "bold")),
            locations = list(cells_column_labels(gt::everything()))
            ) %>%
         fmt_missing(columns = everything(), rows = NULL, missing_text = "-") %>%
         cols_label(country = "Country", d_1960s = "1960s", d_1970s = "1970s", d_1980s = "1980s", d_1990s = "1990s", d_2000s = "2000s", d_2010s = "2010s", mean = "Overall") %>%
         # grand_summary_rows(
         #    columns = 2:8,
         #    fns = list(avg = ~mean(.))
         #    ) %>%
         # summary_rows(
         #    groups = NULL,
         #    columns = vars(d_1960s,d_1970s,d_1980s,d_1990s,d_2000s,d_2010s),
         #    fns = list(mean = ~mean(.)),
         #    ) %>%
      gtsave(here("2020-09-01","output","crops.png"))
```